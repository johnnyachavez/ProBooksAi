---
description: Use synced GitHub + local file context when planning or implementing work
alwaysApply: true
---

# GitHub and local work context

When discussing roadmap, bugs, features, refactors, or what to do next:

1. Prefer reading `integrations/work-context.json` if it exists. It lists **all local repository files** under `localWorkFiles` together with **open GitHub pull requests and issues** (generated on the developer machine).
2. If that file is missing or clearly stale, suggest running `.\scripts\sync-workspace.ps1` from the repo root (requires [GitHub CLI](https://cli.github.com/) and `gh auth login`). If **`gh`** auth fails or **`gh repo view`** fails after login, suggest **`gh auth login -h github.com`** again or **`gh repo set-default owner/repo`** from **`git remote get-url origin`** (the script surfaces targeted warnings).
3. In pull requests, use the GitHub **linked issues** syntax in descriptions and commits (`Fixes #123`, `Relates to #456`) so work stays traceable.

The committed sample shape lives at `integrations/work-context.example.json` (minimal fields: four `localWorkFiles` paths — `index.html`, `invoice.html`, `review.html`, `docs/ROADMAP.md`; **pytest** enforces the exact set). A generated `integrations/work-context.json` lists all repo files under `localWorkFiles` and may include additional PR/issue properties returned by **GitHub CLI** (`gh`).

When editing **`docs/issues-backlog.md`**, **`.github/ISSUE_TEMPLATE/config.yml`** (including **Doc index (issues-backlog)** **`about`** ↔ **`issues-backlog.md`** orienting lines), **`.github/ISSUE_TEMPLATE/bug_report.md`** (**Triaging** line: **ROADMAP** snapshot → **Supporting / cross-cutting**, README **Desktop** → **Default database paths** → **Excel workbook** link order, **`test_github_issue_templates_reference_core_docs`**), **`review.html`** (Issues backlog cards, Documentation grid, **Python + desktop** body copy), **`index.html`** / **`invoice.html`** README hint lines, **`probooks/help_epilog.py`** (shared CLI/desktop **`--help`** Excel line), hub **Related docs** / **Other docs** / **README** **Docs** bar Excel segments, the shared **README — Default database paths** hub segment on **ROADMAP** / **BACKLOG** / **CONTRIBUTING** (**`test_hub_docs_related_docs_link_readme_default_database_paths`**), or the shared **issues-backlog.md** blurb on **ROADMAP** / **BACKLOG** / **CONTRIBUTING**, use **`docs/CONTRIBUTING.md`** (**[Continuous integration](https://github.com/johnnyachavez/ProBooksAi/blob/main/docs/CONTRIBUTING.md#continuous-integration)**) and **`tests/test_ci_validate_layout_sync.py`** so contract tests stay green. The **README — Excel workbook template** parenthetical on **ROADMAP** / **BACKLOG** / **CONTRIBUTING** hub **Related docs** / **Other docs** lines must stay verbatim-identical (**`test_hub_docs_related_docs_link_readme_excel_workbook_template`**).

When editing **`README.md`** **`### Default database paths (Windows)`** or the **ROADMAP** implementation snapshot **Why not one `.db` yet** / **#21** database notes, keep **README** ↔ **ROADMAP** aligned per **CONTRIBUTING.md** (**`test_readme_default_database_paths_notes_two_schemas_and_roadmap`**, **`test_roadmap_snapshot_why_not_one_db_points_at_issue_21_inventory`**, **`test_pr_template_lists_readme_default_database_paths_checklist`**, **`test_contributing_ci_documents_readme_default_database_paths_bullet`**). **README** top **Docs** line should include **`#default-database-paths-windows`** (**`test_readme_docs_bar_links_default_database_paths_anchor`**). Root **`review.html`** (**Documentation** card + **Python + desktop** paragraph) must keep **`README.md#default-database-paths-windows`** in sync and keep **`docs/CONTRIBUTING.md#running-tests`** + **`sync-workspace.ps1`** in the **Python + desktop** body (**`test_review_html_readme_default_database_paths_documentation_card`**, **`test_review_html_links_readme_web_shell_and_desktop_anchors`**, **`test_review_html_python_desktop_section_mentions_help_epilog`**). **`index.html`** / **`invoice.html`** README hints (default paths + DDL inventory, **Running Tests** / **`sync-workspace.ps1`**) stay aligned with **CONTRIBUTING.md** (**`test_static_shell_page_sub_mentions_help_epilog`**). When **`probooks/migrations/*.sql`** or **`probooksai/bank_import.py`** bank schema changes, update **`tests/test_issue_21_schema_inventory.py`** and **CONTRIBUTING.md** (**SQLite issue #21** bullet; **`test_contributing_ci_documents_issue_21_schema_inventory_bullet`**, **`test_pr_template_lists_issue_21_schema_inventory_checklist`**).

**Layout validators:** **`scripts/ci_validate_layout.sh`** and **`scripts/ci_validate_layout.ps1`** **`require`** this file (same path in both lists), **`.gitattributes`** (LF **`*.yml`** / **`*.yaml`** / **`*.sh`**), **`probooks/help_epilog.py`** (next to **`probooks/cli.py`**), and **`tests/conftest.py`** (shared pytest fixtures such as **`isolated_branded_app_data_env`** for **`probooks.paths`** / **`get_data_dir`** tests); the GitHub Actions **validate** job runs the **`.sh`** script. **README** **Scripts** explains **Git Bash** / **WSL** vs **`ci_validate_layout.ps1`** when **`bash`** is not on **`PATH`**. **`docs/CONTRIBUTING.md`** **Layout + workflow contracts** documents what **`test_cursor_rule_github_work_context_points_at_contributing_ci`** covers (hub fragments, root **`review.html`**, **`test_ci_validate_layout_sh_require_paths_exist`** for **`ci_validate_layout.sh`** **`require`** paths on disk (**`tests/test_ci_validate_layout_sync.py`**), **`test_layout_validator_hub_cites_require_paths_pytest`** for **`ci.yml`** + layout script header comments citing that test, **`test_layout_validator_windows_bash_path_guidance_in_hub_ci_scripts_and_cursor_rule`** for **Windows** **`PATH`** / **bash** vs **`.ps1`** copy across **README**, **CONTRIBUTING**, **ci.yml** validate comments, layout script headers, and this **Layout validators** paragraph, **`test_readme_ci_validate_layout_bullet_mentions_conftest`** for **README** **Scripts** **`ci_validate_layout`** + **`tests/conftest.py`** + **`.gitattributes`**, **`test_readme_contributing_section_mentions_conftest`** for **README** **`## Contributing`** + **`tests/conftest.py`** + **Scripts** **`ci_validate_layout`** cross-link, **`test_readme_python_cli_section_mentions_backup_restore_online_api`** for **README** **`## Python CLI`** **backup** / **`probooks.backup`**, **`test_readme_docs_bar_links_python_cli_anchor`** for **README** top **Docs** **`#python-cli`**, **`test_hub_docs_link_roadmap_supporting_cross_cutting_issues`** for **ROADMAP** **`#supporting-cross-cutting-issues`** + **`review.html`** cards, and **`test_github_issue_templates_reference_core_docs`** for **`.github/ISSUE_TEMPLATE/`** / **`config.yml`** / **`bug_report.md`** **Triaging**).
