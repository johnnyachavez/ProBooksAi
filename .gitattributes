# Normalize text; keep GitHub Actions / issue templates as LF for Linux CI and stable diffs.
* text=auto

*.yml text eol=lf
*.yaml text eol=lf
*.sh text eol=lf

# Test fixtures: keep PDF bytes exact (no CRLF “normalization”).
tests/fixtures/*.pdf binary
