# Convenience pin file (broader than pyproject.toml runtime deps).
# Prefer editable install for development: pip install -e ".[ci]" (see README.md and docs/CONTRIBUTING.md).

# Runtime dependencies
openpyxl>=3.1.0
PySide6>=6.6.0
openai>=1.51
anthropic>=0.40
pypdf>=4.3
Pillow>=10.4

# Development / testing
pytest>=7.0.0
